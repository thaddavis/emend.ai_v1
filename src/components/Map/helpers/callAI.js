import React from 'react'

export async function callAI(promptTemplate, cb) {
  try {
    // const response = await axios.post({})
    // console.log('response', response)
  } catch (e) {
    console.error(e)
  }
}
